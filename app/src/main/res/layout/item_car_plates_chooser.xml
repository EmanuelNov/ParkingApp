<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:id="@+id/chooserHolder"
    android:layout_height="66dp"
    android:background="#F5F5F5"
    android:paddingVertical="8dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <Button
        android:id="@+id/checkBox"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:layout_marginStart="12dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:background="@drawable/plates_selected_background"
        android:enabled="false"
        />

    <LinearLayout
        android:id="@+id/platesHolder"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@+id/checkBox"
        android:layout_marginStart="16dp">

        <TextView
            android:id="@+id/plates"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/dm_sans"
            android:textColor="#1B1E2C"
            android:textFontWeight="500"
            />
        <TextView
            android:id="@+id/usedLastTimeLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/dm_sans"
            android:textColor="#1B1E2C"
            android:text="*used last time"
            android:textSize="12sp"
            android:textFontWeight="400"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>